[aws]
region =  # input your aws region, eg. ap-sourtheast-1
queue_url =  #sqs que url, eg. https://sqs.${region}.amazonaws.com/${accountID}/${queuename}
dynamodb_table_name =  # dynamodb table which has taskId field as key
bucket_name =  # bucket name
cloudfront =  # cloudfront url, eg. https://${distributionId}.cloudfront.net/